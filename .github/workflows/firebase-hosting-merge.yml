# This file was auto-generated by the Firebase CLI
# Updated for esses-services project

name: Deploy to Firebase Hosting (esses-services)
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "No build step"
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_ESSES_SERVICES }}
          channelId: live
          projectId: esses-services
